<div class="banner banner-full" style="background-image: linear-gradient(0deg, rgba(0,0,0,0.75) 25%, rgba(0,0,0,0.7) 100%), url('<%= image_url "cinema.jpg"%>')">
  <%# <div class="banner-container"> %>
    <h1>MOVIE'S NIGTH</h1>
    <p>Create your own watch list</p>
  <%# </div> %>
  <%# <%= link_to "See all lists", lists_path, class: "btn-capsule btn-frosted mt-2" %>


  <%= link_to "Add list", new_list_path, class: "btn btn-all mt-2" %>

  <div class= "card">
    <% @lists.each do |list| %>
      <%= link_to list.name, list_path(list), class: "card-list" %>
    <% end %>
  </div>
</div>
