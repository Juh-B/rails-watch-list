<h2> New List </h2>

<div>
  <%= simple_form_for(@list) do |f| %>
    <%= f.input :name %>
    <%= f.submit "Create List", class: "btn btn-primary" %>
  <% end %>
</div>

<%= link_to "Back to all the lists", lists_path(@list) %>

<%# button %>
